name: deploy

on:
  push:
    branches:
      - "master"
    paths:
      - "src/**"
      - "public/**"
      - "package.json"
      - "astro.config.ts"
      - "tailwind.config.ts"
      - "tsconfig.json"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: withastro/action@8a656d96866aa37bcce7153755bad5983b2bcaa0 # v6.1.2

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
